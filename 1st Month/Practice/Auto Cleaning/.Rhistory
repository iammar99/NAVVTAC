import pandas as pd
from pydataset import data
url = "https://raw.githubusercontent.com/tidyverse/dplyr/master/data-raw/starwars.csv"
from pydataset import data
url = "https://raw.githubusercontent.com/tidyverse/dplyr/master/data-raw/starwars.csv"
starwars = pd.read_csv(url)
starwars.head()
starwars[["name", "height", "mass"]]
starwars.select_dtypes(include="number")
starwars.filter(regex="^h")
starwars.drop(columns=["species", "homeworld"])
cols = ["name"] + [c for c in starwars.columns if c != "name"]
starwars[cols]
